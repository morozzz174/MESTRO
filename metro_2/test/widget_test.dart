import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (WidgetTester tester) async {
    // Базовый тест для проверки что приложение запускается
    expect(true, isTrue);
  });
}
